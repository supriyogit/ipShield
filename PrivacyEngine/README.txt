The privacy engine implements the Android kernel changes required for
enforcing the privacy policies specified by the user.

It contains all changes made
a. To the application framework layer
b. Scripts to patch the Android OS
c. Service to track the mapping between apps and the registered
listeners.
